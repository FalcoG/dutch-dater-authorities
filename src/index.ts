import output from '../dist/output.json' with { type: 'json' }

export const calculateWaterAuthorityTax = (propertyValue: number, householdSize: number) => {
  console.log('water auth', output)
}

export const waterAuthorityData = output
